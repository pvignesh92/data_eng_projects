bundle:
  name: delta-lake-deepdive

# Include all notebooks and configuration files
sync:
  include:
    - "./notebooks/*.py"
    - "./notebooks/*.sql"
    - "./jobs/*.yml"

include:
  - ./jobs/*.yml
  - ./jobs/hourly_update_insert_delete_replica_job.yml

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: "https://dbc-8fee4618-06dc.cloud.databricks.com"